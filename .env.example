# Alchemy Ethereum mainnet endpoint (prefer wss for live Sync subscriptions).
MAINNET_RPC_URL=wss://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# Optional: override pool config path (defaults to ./pools.toml)
# POOLS_CONFIG=pools.toml
